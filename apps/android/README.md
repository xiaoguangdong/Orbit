# Android App

This directory contains the current Orbit Android application.

## Stack

- Kotlin
- Jetpack Compose
- Room
- WorkManager / local reminders

## Build

```bash
./gradlew :app:assembleDebug
```

If the local machine needs explicit Java or Gradle cache overrides, run the same validated build command from this directory.
